{
  "name": "coa-demo-test",
  "version": "0.0.0",
  "description": "demo test for coa",
  "keywords": [
    "coajs",
    "coa"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs/coa-demo-test",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-demo-test.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json README.md dist",
    "npm-publish": "yarn build && yarn version --patch && cp package.json README.md dist && cd dist && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  }
}
